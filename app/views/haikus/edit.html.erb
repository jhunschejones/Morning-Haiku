<div class="edit-haiku">
  <div class="edit-haiku-form">
    <%= form_with model: @haiku do |f| %>
      <div class="field">
        <label class="label">Title</label>
        <div class="control">
          <%= f.text_field :title, class: "input", placeholder: "Pretentious title..." %>
        </div>
      </div>

      <div class="field">
        <label class="label">Body</label>
        <div class="control">
          <%= f.text_area :body, class: "textarea", placeholder: "Feel inspired..." %>
        </div>
      </div>

      <div class="field is-grouped">
        <div class="control">
          <%= f.submit "Update", class: "button is-link" %>
        </div>
        <div class="control">
          <%= link_to "Cancel", root_path, class: "button is-link is-light" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
